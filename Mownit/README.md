NEWTON S FRACTAL
What it does: 
basically it checks if its possible to get f(x) = 0 for each pixel using Newton`s method ( in 50 iterations, can be changed to another value).
If its possible, then we color the pixel in output image on red, if its not - in lime green.
